import { Typography } from "@mui/material"

const CheckOutPage = ()=>{
    return(
        <>
        <Typography variant="h4">Only logged in users should see this</Typography>
        </>
    )
}
export default CheckOutPage